A simple shell program written in C.

To COMPILE the file:
   Use the gcc compiler with the GNU99 standard as follows:
   $ gcc --std=gnu99 -o smallsh smallsh.c
   This will create an executable called smallsh.

To RUN the executable:
   Enter the file path of the executable. For example:
   $ ./smallsh

